# Computational Serendipity — Dependencies

# LLM Providers (install the one you're using)
openai>=1.30.0
anthropic>=0.30.0

# Web Search
tavily-python>=0.7.0

# Multimodal Input
opencv-python>=4.8.0
sounddevice>=0.5.0
pynput>=1.7.0

# Configuration
pyyaml>=6.0

# Embeddings (for full scoring)
numpy>=1.24.0

# Memory & Vector Storage
chromadb>=0.5.0

# Screen Awareness (optional)
mss>=9.0.0              # fast screenshot capture
Pillow>=10.0.0          # image processing for screenshots
# pygetwindow>=0.0.9    # cross-platform window title (Windows uses ctypes instead)

# Local Embeddings (optional, for offline mode)
# sentence-transformers>=2.2.0
